.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.DS_Store
node_modules/

# Measurement cache — large, regenerable, and machine-specific.
data/cache/

# Transient sweep bookkeeping.
runs/sweep_state.json
runs/scan_*.json

# Analysis runs ARE tracked. They are the point: anyone cloning this repo can read
# the evidence ledger behind every published score without re-running anything, and
# can re-score an old ledger against a changed model to see what moved.
